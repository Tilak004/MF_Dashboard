# Project Rules

## Before Every Commit
- Always test APIs and code changes locally before committing and pushing.
- Run `go build ./...` to ensure the code compiles.
- Test any API endpoints touched using curl or equivalent.
